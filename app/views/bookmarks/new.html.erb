<div class="container">
      <h1>Add a Movie </h1>
      <%= simple_form_for [@list, @bookmark] do |f| %>
        <%= f.association :movie %> %>
        <%= f.text_area :comment %>
        <%= f.submit "Submit", class: "btn btn-primary" %>
      <% end %>
</div>